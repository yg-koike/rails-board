<nav class="navbar navbar-expand-lg navbar-dark bg__header">
  <%= link_to root_path, class: "navbar-brand" do %>
    <h4 class="header__title"><%= icon "fas", "chalkboard" %> 掲示板</h4>
  <% end %>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>

  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item">
        <%= header_link_item('ホーム', root_path) %>
      </li>
      <li class="nav-item">
        <%= header_link_item('掲示板一覧', boards_path) %>
      </li>
      <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown1" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
         ユーザー
        </a>
        <div class="dropdown-menu" aria-labelledby="navbarDropdown1">
          <% if @current_user %>
            <%= link_to 'マイページ', @current_user, class: 'dropdown-item' %>
            <%= link_to 'ログアウト', logout_path, method: :delete, class: 'dropdown-item' %>
          <% else %>
            <%= link_to '登録', new_user_path, class: 'dropdown-item' %>
            <%= link_to 'ログイン', login_path, class: 'dropdown-item' %>
          <% end %>
        </div>
      </li>
      <% if @current_user %>
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown2" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          カテゴリー
          </a>
          <div class="dropdown-menu" aria-labelledby="navbarDropdown2">
            <%= link_to 'カテゴリー作成', new_category_path, class: 'dropdown-item' %>
            <%= link_to 'カテゴリー一覧', categories_path, class: 'dropdown-item' %>
          </div>
        </li>
      <% end %>
    </ul>
    <%= form_with url: search_users_path, method: :get, local: true, class: 'form-inline my-2 my-lg-0' do |f| %>
      <%= f.text_field :user_name, class: 'form-control mr-sm-2', placeholder: "ユーザー検索" %>
      <%= f.submit "\uf002 検索", class: 'btn btn-primary my-2 my-sm-0 fas search__submit' %>
    <% end %>
  </div>
</nav>